services:
  backend:
    container_name: refund-backend
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "127.0.0.1:3001:3000"
    restart: unless-stopped
